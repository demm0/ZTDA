projection implementation in class ztda_cl_trailler unique;
strict ( 1 );
use draft;

define behavior for ZTDA_C_TRAILLER alias Trailler
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}